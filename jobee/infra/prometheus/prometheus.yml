# Sprint 0: Prometheus scrapes only itself. Each service added in
# Sprint 1+ will get its own scrape job pointing at /metrics.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
